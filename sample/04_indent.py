#!/usr/bin/env python

if (q.is_valid()):
    while (q.it != q.strip.end()):

        char_t = q.it;

        if (not ch.isspace()):
        # Indent the code block below
        return q.calc_pos();

        q.it++;
